# Dependencies
node_modules/
node_modules

# Build outputs
dist/
out/
dist-e2e-mac/
dist-e2e-win/
release/
packages/safe-file-publisher-native/build/

# Test coverage reports
coverage/
playwright-report/
test-results/
*.asar
*.blockmap

# Caches and generated metadata
.cache/
.vite/
.eslintcache
.stylelintcache
*.tsbuildinfo

# node-gyp output for the bundled native module, regenerated by postinstall
packages/*/build/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment files
.env
.env.*
!.env.example
!.env.*.example

# Operating system files
.DS_Store
Thumbs.db

# Editors and IDEs
.idea/
.vscode/
*.code-workspace
*.suo
*.ntvs*
*.njsproj
*.sln
*.user
*.swp
*.swo
*~
.project
.classpath
.settings/
.factorypath
*.sublime-project
*.sublime-workspace

# Internal docs
docs/internal/
docs/superpowers/

# Local-only scripts and tooling
local/
.dev-isolate/

# Agent and AI assistant local state
.hallmark/
.codegraph/
.mcp.json
.codex/
.worktree/
.vitest/
.playwright-cli/
.claude/
.ai/
.aider*
.continue/
.cursor/
.windsurf/
.roo/
.gemini/
.qwen/
.crush/
.goose/
.mcp/
.serena/
.taskmaster/

# Locally downloaded CI build artifacts (Windows installer, etc.)
dist-win-artifact/

# SDD scratch (progress ledger, task briefs, reports) — worktree-local, not tracked
.superpowers/
.scratch/
.coding-*/

.worktreeinclude

# Per-platform micromamba is staged by CI at build time (electron-builder extraResources), never committed.
resources/bin/

# Offline default-env bundle (locks + ~350MB pkgs cache) is produced by `npm run stage-default-envs`
# before packaging and published to the CDN; staged at build time, never committed.
resources/default-envs/

# Python bytecode cache generated when the notebook kernel loop scripts are imported/run locally.
__pycache__/
*.pyc
