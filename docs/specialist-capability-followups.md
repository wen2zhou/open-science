# Specialist capability follow-ups

Codex and OpenCode receive the Specialist's allowed-Skill list as runtime guidance. They do not yet
provide a native per-session Skill whitelist equivalent to Claude Code's `skills` session option.
Follow up by evaluating framework-native session isolation when either runtime exposes a supported
per-session capability API; do not treat the current guidance as a security boundary.
